services:
  app:
    deploy:
      resources:
        limits:
          memory: 4G
    volumes:
      - /var/www/html/vendor
      - /var/www/html/node_modules

  db:
    deploy:
      resources:
        limits:
          memory: 2G
